{
  "name": "calendar",
  "displayName": "Calendar",
  "version": "1.0.0",
  "description": "Full-screen calendar display with multi-source integration",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --target=bun --outdir=./dist --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}